Lookpath
-------
Searches for files in the users `$PATH`. Inspired by the `look(1)` command.

### Running
```sh
$ make
$ ./build/lookpath <query>
```

### Installing
```sh
$ make
$ sudo make install
```
